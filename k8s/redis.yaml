apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod-prediction-redis
  labels:
    app: pod-prediction-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pod-prediction-redis
  template:
    metadata:
      labels:
        app: pod-prediction-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: pod-prediction-redis
  labels:
    app: pod-prediction-redis
spec:
  selector:
    app: pod-prediction-redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
